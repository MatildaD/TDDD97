create table user(email text, password text, firstname text, familyname text, gender text, city text, country text, primary key(email)); 
create table messages(toemail text, fromemail text, message text);
